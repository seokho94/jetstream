"""Cluster (spec §4 stage 3 / §5.1): online leader-follower events (τ=0.84,
top-k=20, centroid EMA, 14-day expiry). current identity = top-down taxonomy +
append-only assignment (stable IDs); split/merge/dormant logged explicitly.
"""
