{
  "name": "meridian",
  "private": true,
  "version": "0.0.0",
  "description": "Meridian — the world, zoomed out. Phase 0 monorepo (web + shared; pipeline + api are Python).",
  "workspaces": [
    "shared",
    "web"
  ],
  "scripts": {
    "dev": "npm --workspace @meridian/web run dev",
    "build": "npm --workspace @meridian/web run build",
    "start": "npm --workspace @meridian/web run start",
    "lint": "npm --workspace @meridian/web run lint",
    "typecheck": "npm --workspace @meridian/shared run typecheck && npm --workspace @meridian/web run typecheck"
  }
}
