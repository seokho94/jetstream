{
  "name": "jetstream",
  "private": true,
  "version": "0.0.0",
  "description": "Jetstream — the world, zoomed out. Phase 0 monorepo (web + shared; pipeline + api are Python).",
  "workspaces": [
    "shared",
    "web"
  ],
  "scripts": {
    "dev": "npm --workspace @jetstream/web run dev",
    "build": "npm --workspace @jetstream/web run build",
    "start": "npm --workspace @jetstream/web run start",
    "lint": "npm --workspace @jetstream/web run lint",
    "typecheck": "npm --workspace @jetstream/shared run typecheck && npm --workspace @jetstream/web run typecheck"
  }
}
